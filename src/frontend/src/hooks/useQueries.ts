// This file is intentionally minimal as the game doesn't use backend queries
// All game logic is handled client-side

export function useQueries() {
  // No backend queries needed for this game
  return {};
}
